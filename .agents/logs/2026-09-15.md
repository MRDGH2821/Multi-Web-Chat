# AI-assisted work log — 2026-09-15

## 2026-09-15T21:18:41+10:00 — Compile the app and fix build issues

**Prompt / request:** "try compiling this app and fix stuff along the way in a new branch"

**Author:** MRDGH2821

**Model:** Claude Sonnet 5 via Claude Code

**What was done:**

- Created branch `mihir/fix/build-compile-errors`
- Installed missing Linux system dev packages for the Tauri/WebKitGTK build
  (`glib2-devel gtk3-devel webkit2gtk4.1-devel libsoup3-devel librsvg2-devel`)
- Verified `bun run check` (svelte-check), `bun run build` (vite), `cargo
check`, `cargo build`, and `cargo test` all pass clean
- Diagnosed `tauri:build` bundling failure: linuxdeploy's bundled `strip`
  can't parse the `.relr.dyn` section emitted by this host's newer
  glibc/binutils; worked around with `NO_STRIP=1`, producing AppImage/.deb/.rpm
  bundles successfully. Documented this in `AGENTS.md` troubleshooting.
- Removed genuinely dead code surfaced by `cargo build` warnings:
  `Provider.adapter_file` (unused, duplicated `load_adapter_source`'s
  hardcoded id→file mapping) and four unused JS-eval helper functions in
  `adapter_runtime.rs` (`inject_adapter_script`, `call_set_prompt_js`,
  `call_submit_js`, `call_new_chat_js`) superseded by `run_send`/`run_new_chat`
- Left `PaneStatusKind::Idle` as-is (never constructed by Rust, but is part
  of the serialized wire enum consumed by the Svelte frontend)
